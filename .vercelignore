# The worker is a separate Railway deployment — don't bundle it into Vercel.
/worker/

# Root-level SQL migrations only (NOT src/lib/supabase — that's the client).
# Leading slash anchors the match to the project root.
/supabase/

# Local files
.env.local
*.log
